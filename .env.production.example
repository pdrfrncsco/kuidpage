# Opcao recomendada: frontend e backend em dominios diferentes
VITE_API_URL=https://kuidapi.ndeas.cloud

# Se o frontend e o backend estiverem no mesmo dominio com proxy reverso para /api,
# pode usar em vez disso:
# VITE_API_URL=

